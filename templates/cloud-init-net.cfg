network:
  version: 2
  ethernets:
    id0:
      match:
        macaddress: ${lower(mac)}
      addresses:
      - ${address}
      gateway4: ${gateway}
      nameservers:
        addresses:
        - ${gateway}
      mtu: ${mtu}
