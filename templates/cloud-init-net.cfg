network:
  version: 2
  ethernets:
    id0:
      match:
        name: e*
      addresses:
      - ${address}
      gateway4: ${gateway}
      nameservers:
        addresses:
        - ${gateway}
      mtu: ${mtu}
